{
  "name": "asnx-cli",
  "version": "0.3.0",
  "description": "Agent-first Asana CLI with schema introspection",
  "license": "MIT",
  "author": "mwpryer",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mwpryer/asnx.git"
  },
  "bin": {
    "asnx": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun scripts/build.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt --check",
    "fmt:fix": "oxfmt",
    "format": "bun run lint:fix && bun run fmt:fix",
    "preversion": "bun run typecheck && bun run lint && bun run fmt && bun run test",
    "postversion": "git push --follow-tags"
  },
  "dependencies": {
    "citty": "^0.2.2",
    "valibot": "^1.4.2",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.58.0",
    "oxlint": "^1.73.0"
  },
  "engines": {
    "node": ">=24"
  }
}
